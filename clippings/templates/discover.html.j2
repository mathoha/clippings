{% extends 'layout.html.j2' %}
{% block content %}
<div class="col-md-4">
    {% for cu in clips_users[::3] %}
       <article class="media content-section">
            <div class="media-body">
                
                <p class="article-content">{{ cu[0].clipping }}</p>
                <div class="article-metadata">
                <small class="text-muted">{{ cu[0].author }} - {{ cu[0].title }}</small>
                </div>
                <div class="article-metadata">
                <small class="text-muted">Added by:
                <a href="{{url_for('clips.user_book', username=cu[1].username, title=cu[0].title)}}">{{ cu[1].username }}</a></small>
                <small class="text-muted">on {{ cu[0].date.strftime('%d-%m-%Y') }}</small>
                <img class="rounded-circle article-img" src="{{ url_for('static', filename='profile_pics/' + cu[1].image_file) }}">
                </div>
            </div>
        </article>
    {% endfor %}
 
 </div>    
 <div class="col-md-4">
    {% for cu in clips_users[1::3] %}
       <article class="media content-section">
            <div class="media-body">
                
                <p class="article-content">{{ cu[0].clipping }}</p>
                <div class="article-metadata">
                <small class="text-muted">{{ cu[0].author }} - {{ cu[0].title }}</small>
                </div>
                <div class="article-metadata">
                <small class="text-muted">Added by:
                <a href="{{url_for('clips.user_book', username=cu[1].username, title=cu[0].title)}}">{{ cu[1].username }}</a></small>
                <small class="text-muted">on {{ cu[0].date.strftime('%d-%m-%Y') }}</small>
                <img class="rounded-circle article-img" src="{{ url_for('static', filename='profile_pics/' + cu[1].image_file) }}">
                </div>
            </div>
        </article>
    {% endfor %}
 </div>
 <div class="col-md-4">
    {% for cu in clips_users[2::3] %}
       <article class="media content-section">
            <div class="media-body">
                
                <p class="article-content">{{ cu[0].clipping }}</p>
                <div class="article-metadata">
                <small class="text-muted">{{ cu[0].author }} - {{ cu[0].title }}</small>
                </div>
                <div class="article-metadata">
                <small class="text-muted">Added by:
                <a href="{{url_for('clips.user_book', username=cu[1].username, title=cu[0].title)}}">{{ cu[1].username }}</a></small>
                <small class="text-muted">on {{ cu[0].date.strftime('%d-%m-%Y') }}</small>
                <img class="rounded-circle article-img" src="{{ url_for('static', filename='profile_pics/' + cu[1].image_file) }}">
                </div>
            </div>
        </article>
    {% endfor %}
    
 </div>
 
 <div>
    {% for page_num in clips.iter_pages(left_edge=1, right_edge=1, left_current=1, right_current=2) %}
        {% if page_num %}
            {% if clips.page == page_num %}
                <a class="btn btn-info mb-4" href="{{ url_for('clips.discover', page=page_num)}}">{{page_num}}</a>
            {% else %}
                <a class="btn btn-outline-info mb-4" href="{{ url_for('clips.discover', page=page_num)}}">{{page_num}}</a>
            {% endif %}
            
        {% else %}
            ...
        {% endif %}

    {% endfor %}
 </div>
{% endblock content %}
    